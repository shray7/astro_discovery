import { ThemeDirective } from './theme.directive';

describe('ThemeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
